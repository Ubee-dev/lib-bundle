# Claude.md — k-lib-bundle

## Important Rules

- **ALWAYS update the documentation** (`docs/`) when modifying code. If a constant, configuration value, or behavior changes, the corresponding documentation must be updated in the same commit.
